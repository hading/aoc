(ns {{namespace}}
  (:require
   [had-utils.io :as hio]
   [had-utils.collections :as hc]
   [had-utils.core :as hcore]
   [superstring.core :as str]
   [clojure.set :as s]))

(def test-input nil)
(def input nil)

(defn test-1 [])
(defn main-1 [])

(defn test-2 [])
(defn main-2 [])
